import React from 'react';
import * as Views from '../index';
import './app.css';

interface AppProps {}

export const App = ( {}: AppProps ) => {
    return (
        <div>
            <p>Hello Vending Machine!</p>
        </div>
    );
}